apiVersion: v1
kind: Service
metadata:
  name: {{ include "medalla-prysm.fullname" . }}
  labels:
    {{- include "medalla-prysm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "medalla-prysm.selectorLabels" . | nindent 4 }}
