apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "beacon-node.fullname" . }}
  labels:
    {{- include "beacon-node.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.deployment.replicaCount }}
  selector:
    matchLabels:
      {{- include "beacon-node.selectorLabels" . | nindent 6 }}
  template:
    metadata:
    {{- with .Values.deployment.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      labels:
        {{- include "beacon-node.selectorLabels" . | nindent 8 }}
    spec:
      securityContext:
        {{- toYaml .Values.deployment.podSecurityContext | nindent 8 }}
      volumes:
      - name: node-data
        persistentVolumeClaim:
          claimName: {{ include "beacon-node.fullname" . }}
      - name: configfile
        configMap:
          name: {{ include "beacon-node.fullname" . }}
      containers:
        - name: {{ .Chart.Name }}
          image: {{ .Values.deployment.image.repository }}:{{ .Values.deployment.image.tag }}
          args:
            - "beacon-chain"
            - "--config-file={{ .Values.deployment.nodeConfigDir }}/{{ .Values.deployment.nodeConfigName }}"
          imagePullPolicy: {{ .Values.deployment.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.ports.p2pTcp }}
              protocol: TCP
            - containerPort: {{ .Values.ports.p2pUdp }}
              protocol: UDP
            - name: grpc
              containerPort: {{ .Values.ports.grpc }}
              protocol: TCP
            - name: metrics
              containerPort: {{ .Values.ports.metrics }}
              protocol: TCP
          volumeMounts:
          - name: node-data
            mountPath: {{ .Values.deployment.dataDir }}
          - name: configfile
            mountPath: {{ .Values.deployment.nodeConfigDir }}
            readOnly: true
{{/*          livenessProbe:*/}}
{{/*            httpGet:*/}}
{{/*              path: /metrics*/}}
{{/*              port: metrics*/}}
{{/*          readinessProbe:*/}}
{{/*            httpGet:*/}}
{{/*              path: /metrics*/}}
{{/*              port: metrics*/}}
